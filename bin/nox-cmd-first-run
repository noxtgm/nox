#!/bin/bash

set -e

FIRST_RUN_MODE=~/.local/state/nox/first-run.mode

if [[ -f "$FIRST_RUN_MODE" ]]; then
  rm -f "$FIRST_RUN_MODE"
  $NOX_PATH/install/first-run/battery-monitor.sh
  $NOX_PATH/install/first-run/firewall.sh
  sudo rm -f /etc/sudoers.d/first-run
fi
